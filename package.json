{
  "name": "teambuilding",
  "version": "0.1.3",
  "description": "QR-based TV host and mobile join flow for TeamBuilding sessions",
  "main": "server.js",
  "scripts": {
    "dev": "npm run build && node server.js",
    "start": "node server.js",
    "build": "npm --prefix frontend run build",
    "postinstall": "if [ -z \"$CI\" ] && [ \"$npm_config_omit\" != \"dev\" ]; then npm --prefix frontend install; fi",
    "gcp-build": "npm --prefix frontend ci --include=dev && npm --prefix frontend run build",
    "test": "node --test",
    "frontend:dev": "npm --prefix frontend run dev",
    "frontend:build": "npm --prefix frontend run build",
    "frontend:preview": "npm --prefix frontend run preview"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Attic-at-Night/TeamBuilding.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Attic-at-Night/TeamBuilding/issues"
  },
  "homepage": "https://github.com/Attic-at-Night/TeamBuilding#readme",
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "qrcode": "^1.5.4",
    "ws": "^8.21.0"
  }
}
